import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

/**
 * No resources are defined for the Gas Oracle MCP yet.
 *
 * This is a placeholder to keep parity with the NullShot MCP template
 * and make it easy to add shared resources later if needed.
 */
export function setupServerResources(_server: McpServer) {
  // Intentionally empty for now.
}