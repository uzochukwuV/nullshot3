import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

/**
 * No resources are defined for the Route Optimizer MCP yet.
 *
 * Route optimization is implemented entirely as tools for now.
 * This placeholder mirrors the NullShot MCP template structure.
 */
export function setupServerResources(_server: McpServer) {
  // Intentionally empty for now.
}