import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

/**
 * No-op resources setup for the Bridge Router MCP.
 *
 * This MCP focuses on tools for interacting with thirdweb Bridge APIs.
 * You can extend this later to expose reusable resources (e.g., common
 * chain/token presets, documentation snippets, etc.).
 */
export function setupServerResources(_server: McpServer) {
  // No resources defined for now.
}